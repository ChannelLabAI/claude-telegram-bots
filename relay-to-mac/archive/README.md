# Completed VPS → Mac relay envelopes

The Mac SessionStart consumer moves successfully handled envelopes here without
changing their bytes or filename. Files in this directory are immutable audit
records and are never re-consumed.
